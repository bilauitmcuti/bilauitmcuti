# table

2026-07-06, golden pair check via CLI (`shadcn add table --diff`), already aligned — no overwrite

## Changed

- None (`shadcn add table` reported skip / no changes)

## Left alone

- [`components/ui/table.tsx`](components/ui/table.tsx) — identical to base-vega registry
- Consumer: [`components/ui/markdown-renderer.tsx`](components/ui/markdown-renderer.tsx)

## Behavior changes

None.

## Verify by hand

- Chat reply containing a markdown table renders correctly
